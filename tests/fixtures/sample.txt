Raft 是一种用于管理复制日志的一致性算法。相比 Paxos，
Raft 结构上更简单，更易于理解和实现。

Raft 将一致性问题分解为三个相对独立的子问题：
1. 领导者选举：当现有 Leader 失效时，集群必须选举新 Leader
2. 日志复制：Leader 接收客户端请求并复制到 Follower
3. 安全性：保证所有节点的状态机以相同顺序应用相同的日志条目

在 Raft 中，时间被划分为任意长度的任期（Term），每个任期由单调递增的整数标识。
每个任期以选举开始，如果选举失败则立即开始新一轮选举。
Leader 在整个任期内保持地位，除非发现更高任期的 Leader。